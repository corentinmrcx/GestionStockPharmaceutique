<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">

        <title>Pharmacie - {% block title %}{% endblock %}</title>

        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
        <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
        <link href="https://fonts.googleapis.com/css2?family=Arsenal:wght@400;500&family=Palanquin:wght@400;500&family=Rubik:wght@400;700&display=swap" rel="stylesheet">

        <link rel="stylesheet" href="{{ asset('styles/styles.css') }}">
        {% block stylesheets %}
        {% endblock %}

        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
        {% block javascripts %}
            {{ importmap() }}
        {% endblock %}
    </head>
    <body>
        <nav class="navbar navbar-expand-lg navbar-custom">
            <div class="container">
                <a class="navbar-brand" href="{{ path('app_index') }}">
                    <img src="{{ asset('images/logo.png') }}" alt="Logo de l'entreprise">
                </a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                    <span class="navbar-toggler-icon"></span>
                </button>

                <div class="collapse navbar-collapse" id="navbarNav">
                    <div class="search-bar-container">
                        <div class="search-bar">
                            <form method="GET" action="{{ path('app_product') }}">
                                <input class="search-input" type="search" placeholder="Rechercher un produit.." aria-label="Search" name="search">
                                <button type="submit" class="search-bar-icon">
                                    <i class="fas fa-search"></i>
                                </button>
                            </form>
                        </div>
                        <ul class="navbar-nav me-auto mb-2 mb-lg-0" id="link_nav">
                            <li class="nav-item">
                                <a class="nav-link" href="{{ path('app_product') }}">Catalogue</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="{{ path('app_category') }}">Catégories</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="#">À propos</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="#">FAQ</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="#">Contact</a>
                            </li>
                        </ul>
                    </div>
                    <div class="action-buttons" id="action-buttons-in">
                        <a href="{{ app.user ? path('app_login') : path('app_login') }}" class="btn">
                            <i class="fas fa-user"></i>
                            {{ app.user ? app.user.firstName : 'Se connecter' }}
                        </a>
                        <a href="#" class="btn">
                            <i class="fas fa-shopping-cart"></i>
                            Panier
                        </a>
                    </div>
                </div>

                <div class="action-buttons" id="action-buttons-out">
                    <a href="{{ app.user ? path('app_login') : path('app_login') }}" class="btn">
                        <i class="fas fa-user"></i>
                        {{ app.user ? app.user.firstName : 'Se connecter' }}
                    </a>
                    <a href="#" class="btn">
                        <i class="fas fa-shopping-cart"></i>
                        Panier
                    </a>
                </div>
            </div>
        </nav>
        <div class="banner">
            Livraison en 24/48h, offerte dès 59€ d'achat !
        </div>

        {% block body %}

        {% endblock %}
    </body>
    <footer class="footer py-4">
        <div class="footer-container text-center">
            <nav>
                <ul class="footer-links d-inline-flex list-unstyled mb-2">
                    <li class="mx-3"><a href="#" class="nav-link">Catalogue</a></li>
                    <li class="mx-3"><a class="nav-link" href="{{ path('app_category') }}">Catégories</a></li>
                    <li class="mx-3"><a href="#" class="nav-link">À propos</a></li>
                    <li class="mx-3"><a href="#" class="nav-link">FAQ</a></li>
                    <li class="mx-3"><a href="{{ path('app_product') }}" class="nav-link">Contact</a></li>
                </ul>
            </nav>
            <hr class="footer-divider">
            <p class="footer-copyright mb-0">
                © 2024 Company, Inc
            </p>
        </div>
    </footer>
</html>
