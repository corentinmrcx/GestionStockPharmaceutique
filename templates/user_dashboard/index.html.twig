{% extends 'base.html.twig' %}

{% block title %}Dashboard{% endblock %}

{% block body %}
    <div class="dashboard-container py-5">
        <h1 class="text-center mb-4">Tableau de Bord</h1>
        <div class="row g-4">

        {% if role == 'ROLE_USER' %}
            <div class="col-md-4">
                <a href="{{ path('app_order') }}" class="dashboardLink">
                    <div class="dashboard-card card">
                        <div class="card-body text-center">
                            <i class="fas fa-cubes fa-3x mb-3"></i>
                            <h5 class="card-title">Mes commandes</h5>
                            <p class="card-text">Accédez à l'historique de mes commandes</p>
                        </div>
                    </div>
                </a>
            </div>
            <div class="col-md-4">
                <a href="{{ path('app_user_edit', {'id' : user.id}) }}" class="dashboardLink">
                    <div class="dashboard-card card">
                        <div class="card-body text-center">
                            <i class="fas fa-gear fa-3x mb-3"></i>
                            <h5 class="card-title">&Eacute;diter mon Profil</h5>
                            <p class="card-text">Accédez à la page de configuration de votre compte</p>
                        </div>
                    </div>
                </a>
            </div>
            <div class="col-md-4">
                <a href="{{ path('app_user_edit_password', {'id' : user.id}) }}" class="dashboardLink">
                    <div class="dashboard-card card">
                        <div class="card-body text-center">
                            <i class="fas fa-key fa-3x mb-3"></i>
                            <h5 class="card-title">&Eacute;diter mon Mot de passe</h5>
                            <p class="card-text">Accédez à la page de configuration du mot de passe de votre compte</p>
                        </div>
                    </div>
                </a>
            </div>
            <div class="col-md-4">
                <a href="{{ path('app_login') }}" class="dashboardLink">
                    <div class="dashboard-card card">
                        <div class="card-body text-center">
                            <i class="fas fa-right-from-bracket fa-3x mb-3"></i>
                            <h5 class="card-title">Déconnexion</h5>
                            <p class="card-text">Accédez à la page de déconnexion</p>
                        </div>
                    </div>
                </a>
            </div>
        {% endif %}

        {% if role == 'ROLE_MANAGER' %}
            <div class="col-md-4">
                <a href="{{ path('app_manager_index') }}" class="dashboardLink">
                    <div class="dashboard-card card">
                        <div class="card-body text-center">
                            <i class="fas fa-users fa-3x mb-3"></i>
                            <h5 class="card-title">DashBoard Gestionnaire</h5>
                            <p class="card-text">Accédez aux outils de gestion des produits et commandes.</p>
                        </div>
                    </div>
                </a>
            </div>
            <div class="col-md-4">
                <a href="{{ path('app_order') }}" class="dashboardLink">
                    <div class="dashboard-card card">
                        <div class="card-body text-center">
                            <i class="fas fa-cubes fa-3x mb-3"></i>
                            <h5 class="card-title">Mes commandes</h5>
                            <p class="card-text">Accédez à l'historique de mes commandes</p>
                        </div>
                    </div>
                </a>
            </div>
            <div class="col-md-4">
                <a href="{{ path('app_user_edit', {'id' : user.id}) }}" class="dashboardLink">
                    <div class="dashboard-card card">
                        <div class="card-body text-center">
                            <i class="fas fa-gear fa-3x mb-3"></i>
                            <h5 class="card-title">&Eacute;diter mon Profil</h5>
                            <p class="card-text">Accédez à la page de configuration de votre compte</p>
                        </div>
                    </div>
                </a>
            </div>
            <div class="col-md-4">
                <a href="{{ path('app_user_edit_password', {'id' : user.id}) }}" class="dashboardLink">
                    <div class="dashboard-card card">
                        <div class="card-body text-center">
                            <i class="fas fa-key fa-3x mb-3"></i>
                            <h5 class="card-title">&Eacute;diter mon Mot de passe</h5>
                            <p class="card-text">Accédez à la page de configuration du mot de passe de votre compte</p>
                        </div>
                    </div>
                </a>
            </div>
            <div class="col-md-4">
                <a href="{{ path('app_login') }}" class="dashboardLink">
                    <div class="dashboard-card card">
                        <div class="card-body text-center">
                            <i class="fas fa-right-from-bracket fa-3x mb-3"></i>
                            <h5 class="card-title">Déconnexion</h5>
                            <p class="card-text">Accédez à la page de déconnexion</p>
                        </div>
                    </div>
                </a>
            </div>
        {% endif %}

        {% if role == 'ROLE_ADMIN' %}
            <div class="col-md-4">
                <a href="{{ path('app_admin_index') }}" class="dashboardLink">
                    <div class="dashboard-card card">
                        <div class="card-body text-center">
                            <i class="fas fa-crown fa-3x mb-3"></i>
                            <h5 class="card-title">DashBoard Administrateur</h5>
                            <p class="card-text">Accédez aux outils d'administration.</p>
                        </div>
                    </div>
                </a>
            </div>
            <div class="col-md-4">
                <a href="{{ path('app_order') }}" class="dashboardLink">
                    <div class="dashboard-card card">
                        <div class="card-body text-center">
                            <i class="fas fa-cubes fa-3x mb-3"></i>
                            <h5 class="card-title">Mes commandes</h5>
                            <p class="card-text">Accédez à l'historique de mes commandes</p>
                        </div>
                    </div>
                </a>
            </div>
            <div class="col-md-4">
                <a href="{{ path('app_user_edit', {'id' : user.id}) }}" class="dashboardLink">
                    <div class="dashboard-card card">
                        <div class="card-body text-center">
                            <i class="fas fa-gear fa-3x mb-3"></i>
                            <h5 class="card-title">&Eacute;diter mon Profil</h5>
                            <p class="card-text">Accédez à la page de configuration de votre compte</p>
                        </div>
                    </div>
                </a>
            </div>
            <div class="col-md-4">
                <a href="{{ path('app_user_edit_password', {'id' : user.id}) }}" class="dashboardLink">
                    <div class="dashboard-card card">
                        <div class="card-body text-center">
                            <i class="fas fa-key fa-3x mb-3"></i>
                            <h5 class="card-title">&Eacute;diter mon Mot de passe</h5>
                            <p class="card-text">Accédez à la page de configuration du mot de passe de votre compte</p>
                        </div>
                    </div>
                </a>
            </div>
            <div class="col-md-4">
                <a href="{{ path('app_login') }}" class="dashboardLink">
                    <div class="dashboard-card card">
                        <div class="card-body text-center">
                            <i class="fas fa-right-from-bracket fa-3x mb-3"></i>
                            <h5 class="card-title">Déconnexion</h5>
                            <p class="card-text">Accédez à la page de déconnexion</p>
                        </div>
                    </div>
                </a>
            </div>
        {% endif %}
    </div>
{% endblock %}