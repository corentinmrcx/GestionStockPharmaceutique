<nav class="navbar navbar-expand-lg navbar-custom">
    <div class="container">
        <a class="navbar-brand" href="{{ path('app_index') }}">
            <img src="{{ asset('images/logo.png') }}" alt="Logo de l'entreprise">
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarNav">
            <div class="search-bar-container">
                <div class="search-bar">
                    <form method="GET" action="{{ path('app_product') }}">
                        <input class="search-input" type="search" placeholder="Rechercher un produit.." aria-label="Search" name="search">
                        <button type="submit" class="search-bar-icon">
                            <i class="fas fa-search"></i>
                        </button>
                    </form>
                </div>
                <ul class="navbar-nav me-auto mb-2 mb-lg-0" id="link_nav">
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_product') }}">Catalogue</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_category') }}">Catégories</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_apropos') }}">À propos</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_faq') }}">FAQ</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_contact') }}">Contact</a>
                    </li>
                </ul>
            </div>
            <div class="action-buttons" id="action-buttons-in">
                <a href="{{ app.user ? path('app_user_dashboard') : path('app_login') }}" class="btn">
                    <i class="fas fa-user"></i>
                    {{ app.user ? app.user.firstName : 'Se connecter' }}
                </a>

                <a href="{{ path('app_cart_index') }}" class="btn">
                    <i class="fas fa-shopping-cart"></i>
                    Panier
                    {% if itemCount > 0 %}
                        <span class="badge bg-primary">{{ itemCount }}</span>
                    {% endif %}
                </a>
            </div>
        </div>

        <div class="action-buttons" id="action-buttons-out">
            {% if app.user and 'ROLE_CUSTOMER' not in app.user.roles %}
                {% if 'ROLE_ADMIN' in app.user.roles %}
                    <a href="{{ path('app_admin_index') }}" class="btn">
                        <i class="fas fa-crown"></i>
                    </a>
                {% elseif 'ROLE_MANAGER' in app.user.roles %}
                    <a href="{{ path('app_manager_index') }}" class="btn">
                        <i class="fas fa-clipboard-list"></i>
                    </a>
                {% endif %}
            {% endif %}
            <div style="display: flex; flex-direction: column; gap: 10px;">
                <a href="{{ app.user ? path('app_user_dashboard') : path('app_login') }}" class="btn">
                    <i class="fas fa-user"></i>
                    {{ app.user ? app.user.firstName : 'Se connecter' }}
                </a>

                <a href="{{ path('app_cart_index') }}" class="btn">
                    <i class="fas fa-shopping-cart"></i>
                    Panier
                    {% if itemCount > 0 %}
                        <span class="badge bg-primary">{{ itemCount }}</span>
                    {% endif %}
                </a>
            </div>
        </div>
    </div>
</nav>